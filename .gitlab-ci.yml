stages:
  - lint

lint-job:
  stage: lint
  script:
    - cp ./materials/linters/.clang-format ./src/.clang-format
    - cd ./src/
    - if [ -f *.cc ] || [ -f *.h ] || [ -f .*cpp ] || [ -f *.tpp ] || [ -f *.hpp ] || [ -f *.c ]; clang-format -n *.cc *.hpp *.cpp *.tpp *.h *.c; fi
