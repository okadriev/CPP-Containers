stages:
  - lint

lint-job:
  stage: lint
  script:
    - cp ./materials/linters/.clang-format ./src/.clang-format
    - cd ./src/
    - if [ $(find . -name "*.cpp" -name "*.hpp" -name "*.tpp" -name "*.cc" -name "*.c" -name "*.h" -exec clang-format -n {} \;) ]; then exit 1; fi
